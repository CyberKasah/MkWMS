<UserControl x:Class="MkWMS.Desktop.Views.UserControls.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="Обновить отчёты" Command="{Binding RefreshCommand}" Padding="14,8"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Остатки" Margin="0,0,0,12">
            <DataGrid ItemsSource="{Binding StockBalances}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Товар" Binding="{Binding Product}" Width="*"/>
                    <DataGridTextColumn Header="Склад" Binding="{Binding Warehouse}" Width="180"/>
                    <DataGridTextColumn Header="Партия" Binding="{Binding Batch}" Width="140"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="120"/>
                    <DataGridTextColumn Header="Ед." Binding="{Binding Unit}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Движения">
            <DataGrid ItemsSource="{Binding Movements}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy HH:mm}" Width="160"/>
                    <DataGridTextColumn Header="Документ" Binding="{Binding Document}" Width="180"/>
                    <DataGridTextColumn Header="Товар" Binding="{Binding Product}" Width="*"/>
                    <DataGridTextColumn Header="Склад" Binding="{Binding Warehouse}" Width="160"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="120"/>
                    <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <TextBlock Grid.Row="3"
                   Margin="0,12,0,0"
                   Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource Danger}"/>
    </Grid>
</UserControl>