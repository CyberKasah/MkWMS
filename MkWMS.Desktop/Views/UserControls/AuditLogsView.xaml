<UserControl x:Class="MkWMS.Desktop.Views.UserControls.AuditLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Width="280" Padding="10" Margin="0,0,12,0"/>
            <Button Content="Поиск" Command="{Binding LoadAsyncCommand}" Padding="14,8" Margin="0,0,12,0"/>
            <Button Content="Обновить" Command="{Binding RefreshCommand}" Padding="14,8"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding AuditLogs}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Пользователь" Binding="{Binding UserId}" Width="120"/>
                <DataGridTextColumn Header="Действие" Binding="{Binding Action}" Width="*"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding ActionDate, StringFormat=dd.MM.yyyy HH:mm:ss}" Width="190"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource Danger}"/>
    </Grid>
</UserControl>