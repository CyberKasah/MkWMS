<UserControl x:Class="MkWMS.Desktop.Views.UserControls.DocumentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Width="280" Padding="10" Margin="0,0,12,0"/>
            <Button Content="Поиск" Command="{Binding LoadAsyncCommand}" Padding="14,8" Margin="0,0,12,0"/>
            <Button Content="Новый документ" Command="{Binding CreateNewDocumentCommand}" Padding="14,8" Margin="0,0,12,0"/>
            <Button Content="Провести" Command="{Binding PostDocumentCommand}" Padding="14,8" Margin="0,0,12,0"/>
            <Button Content="Распровести" Command="{Binding UnpostDocumentCommand}" Padding="14,8"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Documents}"
                  SelectedItem="{Binding SelectedDocument, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                <DataGridTextColumn Header="Номер" Binding="{Binding DocumentNumber}" Width="160"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="130"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding CreatedDate, StringFormat=dd.MM.yyyy HH:mm}" Width="170"/>
                <DataGridTextColumn Header="Склад" Binding="{Binding WarehouseId}" Width="90"/>
                <DataGridTextColumn Header="Подразделение" Binding="{Binding DepartmentId}" Width="120"/>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding Comment}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="{StaticResource Danger}"/>
    </Grid>
</UserControl>