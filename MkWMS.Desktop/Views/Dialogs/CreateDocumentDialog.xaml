<Window x:Class="MkWMS.Desktop.Views.Dialogs.CreateDocumentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создание документа"
        Height="720" Width="980"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{StaticResource Background}">

    <Border Background="{StaticResource Surface}"
            CornerRadius="16"
            Margin="16"
            Effect="{StaticResource Elevation3}">

        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" Text="Создание нового документа"
                       Style="{StaticResource HeaderText}"
                       Margin="0,0,0,24"/>

            <!-- Шапка документа -->
            <Grid Grid.Row="1" Margin="0,0,0,32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Номер (автогенерируется) -->
                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <TextBlock Text="Номер документа" Style="{StaticResource SubHeaderText}"/>
                    <Border Style="{StaticResource InputBorder}" Margin="0,6,0,0">
                        <TextBox Text="{Binding Document.Number}" IsReadOnly="True" Background="{StaticResource SurfaceHover}"/>
                    </Border>
                </StackPanel>

                <!-- Тип документа -->
                <StackPanel Grid.Column="1" Margin="0,0,16,0">
                    <TextBlock Text="Тип документа" Style="{StaticResource SubHeaderText}"/>
                    <ComboBox ItemsSource="{Binding DocumentTypes}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="Id"
                              SelectedValue="{Binding Document.DocumentTypeId}"
                              Style="{StaticResource ComboBoxStyle}"
                              Margin="0,6,0,0"/>
                </StackPanel>

                <!-- Склад -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Склад" Style="{StaticResource SubHeaderText}"/>
                    <ComboBox ItemsSource="{Binding Warehouses}"
                              DisplayMemberPath="Name"
                              SelectedValuePath="Id"
                              SelectedValue="{Binding Document.WarehouseId}"
                              Style="{StaticResource ComboBoxStyle}"
                              Margin="0,6,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Таблица строк -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Строки документа" Style="{StaticResource SubHeaderText}"/>
                    <Button Content="Добавить строку"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding AddItemCommand}"
                            Margin="24,0,0,0"/>
                </StackPanel>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Document.Items}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          Margin="0,0,0,16">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="№" Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Header}"/>
                        <DataGridTemplateColumn Header="Товар" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.Products, RelativeSource={RelativeSource AncestorType=Window}}"
                                              DisplayMemberPath="Name"
                                              SelectedValuePath="Id"
                                              SelectedValue="{Binding ProductId}"
                                              Margin="4"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Количество" Binding="{Binding Quantity, StringFormat={}{0:N2}}" Width="120"/>
                        <DataGridTextColumn Header="Партия" Binding="{Binding BatchId}" Width="100"/>
                        <DataGridTextColumn Header="Серийный №" Binding="{Binding SerialNumberId}" Width="120"/>
                        <DataGridTemplateColumn Header="Действия" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Удалить"
                                            Style="{StaticResource DangerButton}"
                                            Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- Кнопки и статус -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button Content="Сохранить документ"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding SaveCommand}"
                        Width="180" Height="44"
                        Margin="0,0,16,0"/>

                <Button Content="Отмена"
                        Style="{StaticResource OutlineButton}"
                        Command="{Binding CancelCommand}"
                        Width="120" Height="44"/>
            </StackPanel>

            <ProgressBar IsIndeterminate="{Binding IsBusy}"
                         Height="4" Margin="0,16,0,0"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{StaticResource Danger}"
                       TextWrapping="Wrap"
                       Margin="0,12,0,0"
                       HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</Window>